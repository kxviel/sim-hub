# Calculator CSV examples

These files are supplemental, user-downloadable examples selected from source's
`sample_inputs` directory. Each selected file was checked against the matching
parameter schema before being exposed in the frontend.

They are examples rather than end-to-end fixtures. The complete upload fixture
sets remain in `test-inputs`.
