# Simulation Hub

A Tauri desktop workspace for configuring, running, and reviewing scientific
simulations across multiple solver backends.

## Development

```bash
pnpm install
pnpm tauri dev
```

Use `pnpm dev` to run only the Vite frontend and `pnpm build` to verify the
production bundle.
