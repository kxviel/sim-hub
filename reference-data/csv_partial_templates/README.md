# CSV partial templates

These files mirror source's backend parameter schemas. They describe accepted
parameter names, data types, required fields, and options used by middle logic.

They are reference data, not complete user-upload CSV files. Keep them separate
from both `test-inputs` and the downloadable examples in `public/examples`.
